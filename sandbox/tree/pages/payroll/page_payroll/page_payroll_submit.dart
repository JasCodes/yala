import 'package:flutter/material.dart';
import 'package:yala/static/style.dart';

class PagePayrollSubmit extends StatelessWidget {
  final int index;
  const PagePayrollSubmit({
    Key key,
    this.index,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Style.backgroundColor,
    );
  }
}
