import 'package:flutter/material.dart';

class TransferTabSections extends StatelessWidget {
  const TransferTabSections({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
