import 'package:flutter/material.dart';
import 'package:yala/static/style.dart';

class PagePayrollDetails extends StatelessWidget {
  final int index;
  const PagePayrollDetails({
    Key key,
    this.index,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Style.backgroundColor,
    );
  }
}
