import 'package:flutter/material.dart';
import 'package:yala/app_bar.dart';

class PageBillPaymentPay extends StatelessWidget {
  const PageBillPaymentPay({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBarX(
        title: 'Bill Payments',
      ),
      body: Container(),
    );
  }
}
