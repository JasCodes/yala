import 'package:flutter/material.dart';
import 'package:yala/widgets/tree/pages/page_dev.dart';

class DevNavigator extends StatelessWidget {
  const DevNavigator({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ViewDev();
  }
}
