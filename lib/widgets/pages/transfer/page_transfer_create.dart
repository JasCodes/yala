import 'package:flutter/material.dart';

class PageTransferCreate extends StatelessWidget {
  const PageTransferCreate({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
