import 'package:flutter/material.dart';
import 'package:yala/widgets/app_bar.dart';

class PagePayrollAdd extends StatelessWidget {
  const PagePayrollAdd({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBarX(
        title: 'Add a New Employee',
      ),
      body: Container(),
    );
  }
}
