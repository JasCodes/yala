import 'package:flutter/material.dart';

class PageInvoiceCreateSendInvoice extends StatelessWidget {
  const PageInvoiceCreateSendInvoice({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
