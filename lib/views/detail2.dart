import 'package:flutter/material.dart';

class ViewDetail2 extends StatelessWidget {
  const ViewDetail2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // backgroundColor: Colors.transparent,
      appBar: AppBar(
        title: Text("Detail2"),
      ),
      body: Container(
        child: null,
        color: Color(0xcc000000),
      ),
    );
  }
}
